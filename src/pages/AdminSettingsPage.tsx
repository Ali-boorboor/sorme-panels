import AdminSettings from "../components/templates/AdminSettings";
import { memo } from "react";

const AdminSettingsPage = memo(() => {
  return <AdminSettings />;
});

export default AdminSettingsPage;
